# FINDINGS.md

시도한 것과 그 결과를 시간순으로 남긴다. **실패도 반드시 기록한다.**

보고서에서 "이 방법은 이러이러한 이유로 실패했다"는 문단은 성공 사례보다 높게 평가받는다.
기록이 없으면 그 문단을 쓸 수 없다.

---

## 작성 형식

네 줄이면 충분하다. 길게 쓰려다 안 쓰는 것보다 짧게라도 남기는 것이 낫다.

```
## [날짜] 제목

- **시도:** 무엇을 해보았는가
- **결과:** 무엇이 나왔는가 (숫자가 있으면 숫자로)
- **해석:** 왜 그렇게 됐다고 보는가
- **다음:** 그래서 어떻게 할 것인가
```

기록할 가치가 있는 것:
- 예상과 다른 측정 결과
- 작동하지 않은 접근과 그 원인
- 파라미터를 바꿨더니 결과가 크게 달라진 지점
- 외부 라이브러리를 추가한 이유
- 설계를 바꾼 이유와 버린 대안
- 재측정한 경우 그 정당한 사유

---

## [예시 · 실제 기록 시 삭제] 2026-08-18 심볼 길이 5ms에서 복조 실패

- **시도:** 전송률을 높이려고 심볼 길이를 20ms에서 5ms로 줄임
- **결과:** 루프백에서는 무오류였으나 실제 마이크 수신에서 BER 0.4까지 상승
- **해석:** 심볼이 짧아지면 주파수 분해능(약 1/T)이 나빠져 두 반송 주파수 구분이 어려워진다. 루프백은 잔향과 기기 응답 지연이 없어 이 문제가 드러나지 않았다.
- **다음:** 반송 주파수 간격을 넓혀 재측정. 그래도 안 되면 5ms를 임계점 근거 데이터로 보고

---

## 2026-08-20 음향 FSK 루프백 BER 테스트용 SNR 값 탐색

- **시도:** `test_ber_rises_as_snr_drops` 테스트에서 SNR을 얼마나 낮춰야 BER이 실제로 오르는지 확인하려고 -15~-50dB 범위를 스윕
- **결과:** -15dB까지는 BER 0 유지, -20dB에서 BER≈0.066, -25dB에서 BER≈0.217, -30dB 이하에서는 아예 프리앰블을 못 찾음(디코딩 실패)
- **해석:** 심볼 하나가 샘플 수백~수천 개로 이뤄져 FFT가 그 구간 전체를 코히런트하게 적분하므로, 심볼 단위 판정에 상당한 처리 이득(processing gain)이 생겨 입력 SNR이 낮아도 잘 버틴다. 프리앰블 상관 검출은 더 긴 구간(프리앰블 16비트어치)을 적분하므로 처리 이득이 더 커서, 개별 심볼 판정은 이미 절반 가까이 틀렸어도(-25dB) 프레임 시작 지점은 여전히 찾아낸다. -30dB에서 급격히 무너지는 것은 상관 임계치(0.3)를 못 넘기 시작하는 지점으로 보인다
- **다음:** 이 임계 구간(-20~-30dB)이 실제 마이크 환경(잔향, 비정상 잡음)에서도 비슷하게 나타나는지는 M1 실기기 측정으로 확인해야 한다. 실기기 노이즈는 백색 가우시안이 아니므로 다르게 나올 수 있음

## 2026-08-20 자동화 셸 환경에는 오디오 장치가 없음

- **시도:** `airgap/experiments/acoustic_send.py`·`acoustic_recv.py` 작성 후 Claude Code의 PowerShell 도구로 `sounddevice.query_devices()` 실행
- **결과:** 장치 목록 길이 0. `emit()`/`capture()`를 이 경로로는 실행할 수 없음
- **해석:** 이 실행 경로(에이전트가 띄우는 비대화형 PowerShell)에는 오디오 장치가 연결돼 있지 않거나 접근 권한이 없는 것으로 보인다. 루프백 테스트(`modulate`/`demodulate`만 사용)는 문제없이 통과했으므로 코드 자체의 결함은 아니다
- **다음:** M1의 "실제 스피커 → 마이크 전송 성공" 항목은 학생이 직접 일반 터미널(PowerShell 창을 직접 열어서)에서 `python -m sounddevice`로 장치를 먼저 확인하고 `acoustic_send.py`/`acoustic_recv.py`를 실행해야 한다. CLAUDE.md의 "물리적 검증이 필요한 값은 추측하지 말고 측정 스크립트를 만들어 학생이 재도록 한다" 원칙과 일치

## 2026-08-20 노트북 오디오 드라이버 고장으로 M1 실기기 검증이 막힘 → 파일 경유 대체 경로 추가

- **시도:** 학생 노트북에서 직접 `acoustic_send.py`/`acoustic_recv.py`를 실행해 M1 완료 조건(1m, 10회 중 8회 이상)을 확인하려 했으나, 노트북 자체의 오디오 입출력 장치 드라이버가 고장난 상태라 스피커/마이크에 접근할 수 없음
- **결정:** (1) 드라이버 문제로 M1의 실기기 체크박스는 당장 못 채우지만 M2 착수는 미룬다 → 그대로 진행, (2) 안드로이드 폰 두 대(스피커 폰 + 마이크 폰)로 물리 전송 자체를 대체하는 경로를 마련, (3) 드라이버가 고쳐지면 기존 라이브 sounddevice 경로(Enter 동기화, 두 노트북 또는 노트북+외부 마이크)로 계속 진행할 수 있도록 남겨둠
- **구현:** `acoustic_send.py --out-dir`로 시행별 WAV를 파일로 써내고(스피커 재생 대신), `acoustic_recv.py --in-dir`로 폰이 녹음한 파일들을 이름순으로 읽어 판정한다(마이크 캡처 대신). 두 경로 모두 `AcousticFsk.modulate/demodulate`는 그대로 재사용하므로 물리 전송 방식만 바뀌고 판정 로직은 동일 — 채널 간 공정 비교 원칙(CLAUDE.md 규칙 5)과 같은 이유로, 같은 실행 안에서도 라이브/파일 경유 경로가 판정 기준을 공유하게 했다. 폰 녹음이 44.1kHz가 아니거나 스테레오일 수 있어 `_load_signal()`이 표본율을 리샘플링하고 스테레오를 모노로 평균한다 — 무엇을 바꿨는지는 매 시행 로그에 경고로 남긴다
- **미해결:** 폰 녹음 앱이 m4a/aac로 저장하면 `soundfile`이 못 읽는다(특허 코덱 미지원). WAV로 저장하는 앱을 쓰거나 `ffmpeg`로 변환해야 함 — 아직 실기기로 검증 못함
- **다음:** 안드로이드 두 대로 실측 실행 후 성공률을 여기 이어서 기록. 노트북 드라이버가 고쳐지면 같은 실측을 라이브 경로로도 한 번 더 돌려 두 경로(파일 경유 vs 직접 녹음) 성공률 차이가 있는지 비교 — 다르면 그 이유(폰 마이크 주파수응답, 파일 변환 손실 등)를 밝혀야 함

## 2026-08-20 LT 파운틴 부호 유실률별 복원 오버헤드 1차 측정

- **시도:** `core/fountain.py` 구현(로버스트 솔리톤 분포, 시드 기반 방울 생성, 펼치기 디코더) 후 `experiments/fountain_overhead.py`로 K=40(640바이트, 조각 16바이트), c=0.1, delta=0.05 설정에서 유실률 0~50%를 10%p 간격으로, 조건당 20회씩 측정 (`data/raw/20260820_175752_fountain_overhead/trials.csv`)
- **결과:** 모든 유실률(0%, 10%, ..., 50%)에서 20/20 전부 복원 성공. 복원에 쓰인 방울 수는 유실률과 거의 무관하게 K의 1.8~2.0배 수준(평균 오버헤드: 0%→1.80배, 10%→1.83배, 20%→1.92배, 30%→1.91배, 40%→1.99배, 50%→1.96배)이었고, 유실이 없는 조건(0%)에서도 항상 정확히 72개(=1.80×K)가 필요했다(난수가 개입하는 지점이 유실 판정뿐이라 무손실 조건은 매 시행 결과가 동일함)
- **해석:** M2 완료 조건(30% 유실에도 복원)은 여유 있게 만족했다. 다만 오버헤드가 이론적 최솟값(K개, 오버헤드 1.0)보다 훨씬 큰 1.8~2.0배인 것은, K=40이 로버스트 솔리톤 분포의 점근적 보장(수백~수천 단위에서 성립)이 잘 통하기엔 작은 값이기 때문으로 보인다. 유실률이 올라가도 오버헤드가 거의 안 오르는 것은 이번 실험이 "복원될 때까지 계속 받는다"는 이상적 가정(무제한 대기, 최대 8×K개까지 허용) 아래에서 쟀기 때문 — 실제 채널에서는 시간 제약이 있으므로 이 가정이 유효한지 확인 필요
- **다음:** (1) K를 100~500 수준으로 올렸을 때 오버헤드가 이론에 가까운 1.1~1.3배로 줄어드는지 확인, (2) c 파라미터(현재 0.1)를 스윕해 오버헤드-복호실패율 트레이드오프 확인, (3) 지금은 순수 알고리즘 측정이라 채널 없이 쟀는데, M1 음향 채널과 실제로 묶어 프레임 유실률을 실측치로 넣고 다시 재는 것이 다음 단계

## 2026-08-20 LT 파운틴 부호 K 스윕 — K가 커질수록 오버헤드가 이론값에 가까워짐

- **시도:** 위 항목의 후속 측정 (1). `chunk_size_bytes=16`, `c=0.1`, `delta=0.05` 고정, `--payload-bytes`로 K=100(1600B), K=200(3200B), K=500(8000B)를 각각 만들어 유실률 0~50%(10%p 간격)를 조건당 20회씩 측정
  (`data/raw/20260820_212454_fountain_overhead`, `20260820_212526_fountain_overhead`, `20260820_212623_fountain_overhead`, 각각 K=100/200/500)
- **결과:** 세 K 모두 전 유실률 구간에서 120/120 복원 성공. 평균 오버헤드(=수신 방울 수/K)는 K=40일 때 1.8~2.0배였던 것이 K=100→1.81배, K=200→1.58배, K=500→1.36배로 K가 커질수록 꾸준히 낮아졌다. 유실률에 따른 오버헤드 변화는 여전히 미미함(K=500 기준 1.33~1.39배 범위)
- **해석:** 로버스트 솔리톤 분포의 점근적 보장(K→∞에서 오버헤드→1에 수렴)이 실측에서도 방향은 맞게 나타난다. 다만 K=500에서도 여전히 1.1~1.3배 예상 범위보다 높은 1.36배인데, `max_k_mult=8`이라는 상한 자체는 도달한 적이 없어서 상한 때문은 아니고, `c=0.1`이 K=500 정도에서는 아직 충분히 점근적 구간에 들지 못한 것으로 보인다(Luby 2002 원 논문의 실험도 K가 수천~수만 단위였음)
- **다음:** K=1000 이상까지 더 밀어붙여 1.1~1.3배 구간에 실제로 도달하는지 확인할 수 있으나, 실제 채널(음향)에서 쓰는 프레임 페이로드가 이보다 훨씬 작아 실용적 의미는 제한적 — 보고서에는 "K가 커질수록 오버헤드가 줄어드는 경향"과 "그래도 실제 사용 규모(K=40 안팎)에서는 1.8~2.0배가 현실적 수치"라는 두 가지를 함께 제시

## 2026-08-20 LT 파운틴 부호 c 스윕 — 오버헤드가 c에 단조적이지 않음

- **시도:** 위 항목의 후속 측정 (2). K=100(`--payload-bytes 1600`) 고정, `c`를 0.03/0.1/0.2/0.5로 바꿔가며(각각 `config/fountain_c003.yaml`, 기존 `config/fountain.yaml`, `config/fountain_c02.yaml`, `config/fountain_c05.yaml`) 유실률 0~50%를 조건당 20회씩 측정
  (`data/raw/20260820_212830_fountain_overhead`(c=0.03), `20260820_212454_fountain_overhead`(c=0.1), `20260820_212906_fountain_overhead`(c=0.2), `20260820_212949_fountain_overhead`(c=0.5))
- **결과:** 네 조건 모두 전 유실률 구간에서 120/120 복원 성공(복호 실패는 한 번도 없었음). 평균 오버헤드는 c=0.03→1.49배, c=0.1→1.81배, c=0.2→1.96배, c=0.5→1.53배로, c가 커질수록 오버헤드가 커지다가 c=0.5에서 다시 떨어지는 **비단조** 패턴이 나왔다
- **해석:** `robust_soliton_distribution`에서 스파이크 폭 `S = c*sqrt(K)*log(K/delta)`와 그 스파이크가 몰리는 차수 `boundary = round(K/S)`가 정수 반올림을 거치는데, K=100처럼 작은 K에서는 c가 조금만 바뀌어도 boundary가 정수 단위로 홱홱 뛴다. 그 결과 "c가 크면 항상 오버헤드도 크다"는 단순한 관계가 K=100 규모에서는 깨지는 것으로 보인다 — c와 오버헤드의 관계가 매끈한 곡선이 아니라 K에 의존하는 계단형이라는 뜻. delta=0.05 고정이라 복호 실패 확률 상한 자체는 네 조건 다 이론상 5%인데 실측으로는 4개 조건 모두 0%였다(20회는 5% 실패율을 유의미하게 검증하기엔 표본이 작음 — 반복 횟수를 늘려야 진짜 delta 근처인지 확인 가능)
- **다음:** c와 오버헤드의 관계를 논할 때 "K=100 기준"이라는 조건을 반드시 명시. delta로 예측한 복호실패율을 실측으로 검증하려면 반복 횟수를 최소 수백 회로 늘려야 하는데, 이번 M2 범위에서는 우선순위가 낮음(시간 대비 얻는 정보가 적음) — 확장 여유가 있으면 시도

## 2026-08-20 파운틴 부호를 화면 QR 채널에 실제로 연결 — base64 우회 필요했음

- **시도:** M3 착수(사용자 승인). `channels/screen_flicker.py`(밝기 변조)·`channels/screen_qr.py`(QR)를 `core/channel.py` 인터페이스로 구현하고 `tests/test_channels.py`의 `CHANNEL_FACTORIES`에 등록해 루프백 테스트를 자동 적용. 이어서 `experiments/fountain_over_qr.py`로 LT 방울을 실제 `frame.build_frame()` → `ScreenQr.modulate()`(QR 이미지 생성) → `demodulate()`(zbar 인식) → `LtDecoder.add_droplet()` 전 계층에 통과시켜, "매 프레임마다 바뀌는 QR 코드"가 파운틴 부호를 실제로 실어 나르는지 확인 (K=10, 유실률 0~30%, 20회씩, `data/raw/20260820_214944_fountain_over_qr`)
- **결과:** (1) QR 채널 첫 구현에서 프리앰블·CRC 바이트(0x80 이상의 값)가 `qrcode`→`pyzbar` 왕복을 거치며 다른 바이트로 깨짐 — 프레임 페이로드를 base64로 감싸서 해결(라이브러리 문제 상세는 해석 참고). (2) 수정 후 전 채널 루프백 테스트(11개) 통과. (3) 파운틴+QR 통합 실험은 유실률 0~30% 전 구간에서 80/80 복원 성공, QR 자체의 인식 실패는 0건(순수 소프트웨어 루프백이라 광학적 잡음이 없어 당연함), 평균 오버헤드는 K=10 기준 2.2~2.6배(방금 K 스윕에서 확인한 "K가 작을수록 오버헤드가 크다"는 경향과 일치)
- **해석:** 원인은 `zbar`(pyzbar가 감싸는 C 라이브러리)가 QR "8비트 바이트 모드" 내용을 텍스트로 간주해 문자 인코딩을 추정(Shift-JIS 계열)한 뒤 UTF-8로 재인코딩해서 돌려주는 동작 때문이었다. 예: 바이트 `0xf9 0xab`를 그대로 담아 QR을 만들고 다시 읽으면 `0xee 0x9c 0x86`이 나옴 — 유효한 SJIS 2바이트 문자로 오인되어 재인코딩된 것. 우리 프레임은 이진 프로토콜(프리앰블·CRC가 임의의 바이트값)이라 이 "텍스트로 보이게 하려는" 동작과 정면으로 부딪힌다. base64로 감싸면 QR에 담기는 내용이 전부 ASCII라 이 오작동이 발생할 여지가 없어진다 — 원본의 약 4/3배로 용량이 늘지만 우리 프레임은 작아서(K=40 기준 방울 하나가 20바이트 안팎) 문제되지 않는다
- **다음:** 지금은 modulate→demodulate 직결(카메라 없는 루프백)까지만 확인했다. 실제 카메라·화면으로 검증하는 실기기 스크립트(`experiments/screen_qr_send.py`/`recv.py`)를 M1의 acoustic_send/recv와 같은 패턴으로 추가해 학생이 두 기기로 재도록 한다. 밝기 변조 채널(`screen_flicker.py`)은 루프백 테스트만 통과한 상태 — QR과의 전송률 비교 측정(M3 완료 조건)은 두 채널 다 실기기 측정이 있어야 의미가 있다

## 2026-08-20 screen_qr_recv.py에 안드로이드 폰 동영상 경유 경로 추가

- **시도:** 수신측을 노트북 웹캠이 아니라 안드로이드 폰으로도 쓸 수 있게 해달라는 요청. 이 프로젝트 코드가 폰에서 직접 돌지 않으므로, M1의 WAV 파일 경유(방법 B)와 같은 방식으로 "폰 카메라 앱으로 화면을 녹화 → 동영상 파일을 노트북으로 옮김 → 그 파일을 읽어 판정"하는 경로(`--in-dir`)를 추가. `cv2.VideoCapture`가 카메라 장치 번호와 동영상 파일 경로를 똑같이 받아들이는 걸 이용해 프레임을 하나씩 꺼내는 코드(`_receive_one_trial`)는 실시간 카메라 경로와 완전히 공유함
- **결과:** 합성 QR 프레임 15장을 3배씩 반복해 만든 테스트용 mp4(376KB)로 `--in-dir` 경로를 실행해보니 프레임 7개 만에 방울 3개를 모아 원문("AIRGAP 20자 테스트문자열", k=2)을 정확히 복원함. 전 테스트(47개) 통과 유지
- **해석:** 실시간 카메라 여부와 무관하게 "그레이스케일 프레임을 하나씩 내놓는 소스"로만 추상화하면 판정 로직을 공유할 수 있었다 — M1에서 이미 검증된 패턴(라이브 경로·파일 경유 경로가 같은 `_evaluate`를 쓰는 것)을 그대로 재사용한 것. 실제 폰으로 검증한 것은 아니라서 진짜 카메라의 압축 손실·모션 블러·프레임 속도 차이는 아직 반영 안 됨
- **다음:** 학생이 실제 안드로이드 폰으로 노트북 화면을 녹화해 이 경로를 재야 한다(guide.txt 방법 D-2). 실제 동영상에서 프레임 속도(보통 30fps 이상)가 QR 표시 간격(기본 500ms)보다 훨씬 빠르므로, 같은 QR이 여러 프레임에 걸쳐 찍혀도 문제없다는 것까지 실기기로 확인 필요

## 2026-08-21 음향 채널 10cm 실측 — 매우 가까운 거리에서도 성공률 40%에 그침

- **시도:** 태블릿(스피커)-폰(마이크) 10cm 거리, `acoustic_recv.py --in-dir in\recv --distance-cm 10`으로 10회 판정 (`data/raw/20260821_081256_acoustic_manual_relay`)
- **결과:** 4/10 성공(40%) — CRC 불일치 1회, 프리앰블 미검출 5회. 같은 원본 wav를 그보다 앞서 `--distance-cm 100`으로 판정한 실행(`data/raw/20260821_000957_acoustic_manual_relay`)도 있는데, 이건 **거리 라벨을 잘못 입력한 것**(guide.txt 예시 명령어들의 기본값 100을 안 고치고 그대로 실행)일 뿐 실제로 1m에서 딴 적은 없다 — `20260821_000957_...`의 `distance_cm=100`은 오기재이니 무시하고, 실제 거리는 두 실행 모두 10cm로 본다. (CLAUDE.md 규칙 3에 따라 이미 생성된 두 폴더는 그대로 두고 수정하지 않음 — 이 항목으로 오기재 사실만 기록해둔다)
- **해석:** 시행별 진폭(peak/RMS)을 뽑아보니 성공/실패가 진폭 하나로 깔끔하게 갈리지 않았다(진폭 0.32짜리 트라이얼끼리도 성공·실패가 갈림, 클리핑은 전 트라이얼에서 0건). 대신 트라이얼 간 최대진폭이 0.24~0.92배로 4배 가까이 들쭉날쭉했는데, 10cm처럼 가까운 거리에서는 폰·태블릿을 손으로 들고 있으면 미세한 각도·거리 변화가 수신 강도에 크게 영향을 주고(근접장이라 거리 변화에 따른 감쇠가 원거리보다 훨씬 가파름), 마이크 자동 게인(AGC)도 트라이얼마다 다르게 걸렸을 가능성이 있다. `_find_preamble_start`의 정규화 상관 임계치(0.3)가 **수신 신호 에너지가 아니라 송신 파형 에너지로만 정규화**돼 있어(`airgap/channels/acoustic_fsk.py`), 이런 진폭 변동에 그대로 노출된다 — "가까우면 무조건 잘 될 것"이라는 예상과 달리, 가까운 거리에서 오히려 트라이얼 간 재현성이 떨어질 수 있음을 보여주는 사례
- **다음:** (1) M1 완료 조건대로 1m 거리 실측을 진행해 10cm보다 나은지(혹은 비슷한지) 비교 — 근접장 변동성 가설이 맞다면 1m에서는 오히려 더 안정적일 수 있음. (2) 기기를 손으로 들지 않고 거치대 등으로 고정한 채 재측정해 "거리 자체의 한계"와 "손떨림/각도 변화"를 분리해서 볼 필요가 있음. (3) 프리앰블 검출 정규화 방식(송신 에너지 기준 vs 수신 신호 자체 에너지 기준)을 바꾸면 진폭 변동에 덜 민감해질 수 있는데, 이건 핵심 알고리즘 설계 변경이라 임의로 바꾸지 않고 다음 세션에서 학생과 논의 후 결정

## 2026-08-21 acoustic_recv.py --in-dir 경로에 원본 보존·수정시각 로그 추가

- **시도:** 위 10cm 결과를 검토하는 과정에서, `--in-dir` 판정에 실제로 쓰인 원본 wav가 `data/raw`에 전혀 보존되지 않는다는 걸 발견(사후 검증 수단이 없어 한동안 "예전 폴더를 잘못 읽은 것 아니냐"는 엉뚱한 의심까지 나왔었음 — 결국은 오기재였을 뿐 파일 자체는 처음부터 맞았음)
- **결과:** `acoustic_recv.py`가 `--in-dir` 사용 시 (1) 각 파일의 절대경로·수정 시각·크기를 처리 전에 로그로 나열하고, (2) 실제로 읽은 원본 wav 전체를 `data/raw/<run_id>/src/`에 복사해 남기도록 수정. 루프백 포함 전체 테스트(47개) 통과 확인
- **해석:** 이번 사고의 진짜 원인은 폴더 문제가 아니라 명령어의 `--distance-cm` 값을 안 고친 단순 오기재였지만, 그걸 밝혀내는 과정에서 "원본이 안 남으니 사후 검증이 안 된다"는 실질적인 약점이 드러났다. CLAUDE.md 규칙 3(측정 원본 보존)의 취지상 이 경로는 원래도 원본을 남겼어야 했음
- **다음:** `screen_qr_recv.py`도 같은 `--in-dir` 패턴을 쓰므로 같은 보완이 필요할 수 있음 — 다음에 그 스크립트를 손댈 때 같이 적용할 것. 실행 전 `--distance-cm` 등 필수 인자를 콘솔 로그 맨 위에서 항상 확인하는 습관도 필요(guide.txt에 안내 추가함) — **적용 완료(2026-08-21, 아래 항목)**

## 2026-08-21 screen_qr_recv.py --in-dir 경로에도 원본 보존·수정시각 로그 적용

- **시도:** 위 acoustic_recv.py 보완을 `screen_qr_recv.py`(같은 `--in-dir` 동영상 경유 패턴)에도 동일하게 적용
- **결과:** 처리 전 각 동영상 파일의 절대경로·수정 시각·크기를 로그로 나열하고, 실제로 쓴 동영상을 `data/raw/<run_id>/src/`에 복사해 남기도록 수정. 가짜 mp4로 아카이빙·로그가 디코딩 실패보다 먼저 실행되는 것 확인, 전체 테스트 55개 통과 유지
- **해석:** 두 `--in-dir` 스크립트(acoustic_recv.py, screen_qr_recv.py)가 이제 동일한 원본 보존 방식을 쓰게 됐다 — 채널마다 다른 보존 정책을 쓰면 그 자체로 채널 간 공정 비교 원칙(CLAUDE.md 규칙 5)에 어긋날 수 있어서, 한쪽만 고치고 끝내지 않고 짝을 맞춘 것
- **다음:** `screen_flicker_recv.py`는 애초에 만들 때부터(오늘) 이 보완을 포함해서 만들었으므로 세 `--in-dir` 스크립트 모두 동일한 방식으로 통일됨

## 2026-08-21 screen_flicker 실기기 측정 스크립트(send/recv) 작성

- **시도:** M3 남은 항목 중 하나. QR과 달리 밝기 변조는 파운틴 방울을 흩뿌리는 채널이 아니라(coding_axes가 QR은 시간+공간, screen_flicker는 acoustic_fsk와 같이 시간만) 시행마다 프레임 하나를 시간축으로 통째로 보내는 채널이라, `screen_qr_send/recv.py`가 아니라 `acoustic_send/recv.py` 구조(시행마다 Enter로 동기화, 시행 하나 = 프레임 하나)를 그대로 따라 `screen_flicker_send.py`/`screen_flicker_recv.py`를 작성. 수신측은 D-1(실시간 카메라)·D-2(안드로이드 동영상 경유) 두 경로를 QR과 동일하게 지원(guide.txt 방법 E로 정리)
- **결과:** `cv2.VideoWriter`로 합성 동영상(mp4v)을 만들어 modulate→동영상 인코딩→`_sample_video_file`로 디코딩→demodulate 전 경로를 통과시켜보니 정상 복원됨. 동영상 자체의 fps 메타데이터가 설정값과 다른 경우(폰이 24fps로 찍었는데 config는 30fps인 상황을 흉내)도 실제 fps로 다시 복조하도록 만들어 통과 확인. `tests/test_screen_flicker_experiments.py`에 이 두 경로를 회귀 테스트로 남김(음향 채널의 표본율 리샘플링 테스트와 같은 목적). 전체 테스트 51개 통과
- **해석:** 어제(2026-08-21 위 항목) 겪은 `--distance-cm` 오기재 사고를 겪은 직후라, 이번엔 처음부터 원본 동영상 보존(`data/raw/<run_id>/src/`)과 수정 시각 로그를 넣고 시작했다 — 같은 실수를 반복하지 않으려고 사고 대응을 다음 스크립트에 바로 반영한 것
- **다음:** 학생이 실제 카메라로 D-1/D-2에 해당하는 E-1/E-2 경로를 재야 한다(guide.txt 방법 E). 심볼 길이가 기본 100ms라 20자 메시지도 30초 넘게 걸리는데, 이게 QR(파운틴 스트리밍이라 필요한 만큼만 모으면 바로 끝남)과 비교했을 때 "느리지만 안정적인가, 아니면 그만큼 오래 노출돼서 오류가 더 쌓이는가"가 M3 완료 조건(QR과 밝기 변조 전송률 비교)의 핵심 관전 포인트가 될 것

## 2026-08-21 QR 채널 실기기 시연 시 화면이 껐다 켜졌다 깜빡이는 문제

- **시도:** 학생이 `screen_qr_send.py`를 실제로 띄워보니 화면이 계속 껐다 켜졌다 해서 보기 어지럽다고 보고. 원인 확인해보니 `ScreenQr.emit()`이 호출될 때마다 `cv2.namedWindow`로 창을 새로 열고 끝에서 `cv2.destroyWindow`로 바로 닫는 구조였는데, `screen_qr_send.py`는 파운틴 방울(프레임)마다 `emit()`을 반복 호출한다(기본 60번) — 그래서 창이 시행 하나당 최대 60번 열렸다 닫혔다 했음
- **결과:** `ScreenQr`에 `_window_open` 상태를 추가해 첫 `emit()`에서만 창을 열고, 이후로는 `cv2.imshow`로 내용만 갈아끼우도록 수정. 창을 실제로 닫는 건 새로 추가한 `close_display()`가 맡고, `screen_qr_send.py`가 프레임 루프를 `try/finally`로 감싸 끝나면(또는 도중에 중단돼도) 한 번만 호출하도록 함. 전체 테스트 51개 통과 유지
- **해석:** `screen_flicker.py`는 애초에 `emit()` 한 번 호출에 메시지 전체(수백 프레임)를 자체 루프 안에서 처리하고 창을 한 번만 여닫아서 이 문제가 없었다 — 두 채널의 emit() 호출 빈도가 다른(QR은 프레임마다, 밝기변조는 메시지 전체에 한 번) 설계 차이가 원인이었지, 화면 채널 자체의 근본적인 한계는 아니었음
- **다음:** 학생이 실제로 다시 띄워보고 깜빡임이 없어졌는지 확인 — **확인 완료(2026-08-21), 더 이상 안 깜빡임.** 이게 카메라 인식률에도 영향을 주는지(깜빡이지 않으면 초점이 더 잘 맞을 수 있음)는 아직 미확인

## 2026-08-21 M5 데모 프로토타입 — 실시간 음향 채팅(`demo/acoustic_chat.py`)

- **시도:** 학생이 이 프로젝트 아이디어의 영감이 된 릴스 스크린샷 4장을 공유(DECIMEN — 파운틴 QR 전송 실시간 HUD, SOUNDCHAT — 폰 두 대가 소리로 실시간 채팅). 그중 SOUNDCHAT식 "타이핑하는 대로 바로바로 오가는" 실시간성을 원한다고 해서, M5(`demo/`, 발표 시연용)에 해당하는 프로토타입을 일정보다 앞서 만들어봄(학생이 지금 바로 시작하길 원해서 착수)
- **결과:** `demo/acoustic_chat.py` 작성. `AcousticFsk`는 그대로 재사용하고, `sounddevice.InputStream` 콜백으로 마이크를 백그라운드 스레드에서 계속 열어둔 채 오디오를 큐에 쌓고, 0.3초 간격으로만 프리앰블 상관(디코딩)을 시도하도록 함(매 콜백마다 디코딩하면 CPU가 못 버팀). 타이핑한 메시지는 `input()` 루프에서 바로 `emit()`으로 재생. `ChatReceiver`(판정 로직만 분리한 클래스)에 대해 `tests/test_acoustic_chat.py` 4개 작성 — 새 메시지 인식, 자기 반향 필터링, 같은 프레임 중복 보고 방지, 두 기기가 우연히 같은 시드를 써도 내용이 다르면 안 섞이는 것까지 확인. 전체 테스트 55개 통과
- **해석:** SOUNDCHAT 스크린샷의 TX/RX 토글 UI로 미루어 그쪽도 전이중이 아니라 반이중(무전기)일 가능성이 커 보여, 우리도 같은 방식을 택함 — 같은 기기의 스피커·마이크가 물리적으로 가까우면 자기가 낸 소리를 자기 마이크가 다시 듣는 자기 반향 문제가 있는데, 전이중으로 만들려면 에코 제거(echo cancellation)라는 완전히 새로운 신호처리 문제가 추가돼 프로젝트 범위를 크게 벗어난다. 반이중 + "내가 보낸 (시드,내용)은 걸러낸다"는 훨씬 단순한 우회로 같은 효과(양쪽에서 서로의 메시지를 실시간으로 봄)를 냄
- **다음:** 지금까지는 `ChatReceiver`의 판정 로직만 자동 테스트로 확인했고, 실제 마이크·스피커·스레드로 두 기기 간 시연은 아직 아무도 안 해봄. **M3(현재 진행 단계)를 지연시키면 안 되므로, 이 데모는 여기서 잠깐 멈추고 M3 작업으로 돌아간다** — 실기기 시연 검증은 M5 시점에 마저 진행

## 2026-08-21 안드로이드 앱화는 보류 — 노트북 데모로 확정

- **시도:** 학생이 `acoustic_chat.py`를 안드로이드 두 대끼리 돌아가는 진짜 앱으로 만들 수 있는지 문의(처음엔 네이티브 앱, 이어서 Flutter도 제안). 검토해보니 이 컴퓨터에 Android SDK는 이미 설치돼 있었지만(`%LOCALAPPDATA%\Android\Sdk`, adb 포함) Flutter/Android Studio는 없었음
- **결과:** 세 갈래를 검토함 — (1) Flutter/네이티브(Kotlin) 앱: FSK 변복조·CRC·프레임 파싱을 Dart나 Kotlin으로 처음부터 재구현해야 해서 검증된 파이썬 코드와 별개로 다시 테스트를 다 만들어야 하고, 두 구현이 갈라질 위험(CLAUDE.md 규칙 5, 채널 간 공정 비교와 충돌 소지). (2) Chaquopy(안드로이드 Kotlin 앱에 파이썬 인터프리터를 통째로 넣어 numpy까지 그대로 쓰는 방식): 알고리즘 재구현은 피할 수 있지만 Android Studio·Gradle·Kotlin 오디오 연동을 새로 배우고 붙여야 함. 학생이 (1)·(2) 대신 **A안(노트북 데모 유지)**을 선택
- **해석:** 마감(9/10)까지 약 3주 남은 시점에서, 두 방식 다 이 프로젝트의 핵심(채널의 물리적 성질 비교)과 무관한 새 스택(모바일 앱 개발)을 익히는 데 시간을 쓰게 돼 M3~M5 본 작업을 지연시킬 위험이 컸다. `demo/acoustic_chat.py`는 이미 동작하는 실시간 채팅 데모라 발표 시연 목적은 노트북 두 대로도 충분히 달성됨
- **다음:** "안드로이드 두 대끼리"는 확장 과제로도 PLAN.md에 올리지 않음(우선순위 밖) — 시간이 아주 많이 남으면 그때 Chaquopy를 재고할 수는 있지만 지금은 M3로 복귀

## 2026-08-21 작업 파일 정리 — 10cm 실측 원본을 data/raw로 옮기고 스크래치 폴더 비움

- **시도:** 세션 중 쌓인 불필요한 파일 정리(릴스 참고용 스크린샷 4장, `in/recv`·`out/send`의 임시 wav). `in/recv`에는 §"음향 채널 10cm 실측" 항목의 **실제 원본 녹음**이 남아있었는데, 원본 보존 기능(src/ 아카이빙)을 넣은 게 그 실행보다 나중이라 이 폴더가 유일한 사본이었음
- **결과:** `in/recv/*.wav` 10개를 `data/raw/20260821_081256_acoustic_manual_relay/src/`로 복사(개수·파일 크기 일치 확인 후)하고 나서 `in/recv`·`out/send`(재생성 가능한 합성 톤)를 비움. 스크린샷 4장은 삭제(git 추적 대상 아니었고, 내용은 이미 이 문서에 남아 있음)
- **해석:** `in/recv`·`out/send`는 `.gitignore`에도 "안드로이드 중계용 임시" 폴더로 명시돼 있어 원래 스크래치 용도였다 — 실측 원본이 우연히 거기만 남아 있었던 것 자체가 위 아카이빙 기능이 필요했던 이유를 다시 보여준다
- **다음:** 앞으로 실측은 `--in-dir` 실행 시 자동으로 `data/raw/<run_id>/src/`에 보존되므로, `in/`·`out/`는 다음 실측 전까지 비워둬도 안전함

## 2026-08-22 데모 UI Tier 1(rich) 프로토타입 — Windows 콘솔 한글 깨짐 재확인

- **시도:** SNS 참고 영상(DECIMEN, SOUNDCHAT) 수준의 "있어 보이는" 데모 UI를 목표로 잡고(`docs/DEMO_UI_PLAN.md`), 첫 단계로 `demo/demo_hub.py`(상태 저장소)와 `demo/hud_rich.py`(rich 패널 렌더러)를 만들어 `acoustic_chat.py`에 얹음
- **결과:** Git Bash 기본 코드페이지에서 실행하면 패널의 한글·특수문자(●, 박스 테두리)가 전부 깨져 나옴. `chcp 65001` 적용 후에는 정상 출력됨(테두리·색상·한글 모두 정상)
- **해석:** CLAUDE.md에 이미 적혀 있는 "콘솔 한글 깨짐: chcp 65001" 주의사항이 rich처럼 유니코드 박스 문자를 쓰는 라이브러리에서 훨씬 눈에 띄게 나타난다 — 일반 print()의 한글 깨짐은 읽기 불편한 정도지만, rich의 테두리 문자가 깨지면 패널 모양 자체가 무너져서 발표 시연에서는 치명적일 수 있음
- **다음:** `acoustic_chat.py` 사용법 안내에 `chcp 65001` 문구를 추가해둠. 실기기 시연 리허설(M5) 때 학생이 실제로 쓸 PowerShell 창에서도 한 번 확인 필요 — Git Bash와 PowerShell의 기본 코드페이지가 다를 수 있어 여기서 재현된 문제가 그대로 나타난다고 단정할 수는 없음

## 2026-09-03 QR 페이로드를 base64에서 base45로 교체 — 같은 조각이 더 작은 QR에 들어감

- **시도:** QR 채널의 전송률을 올릴 방법을 검토하던 중, 프레임을 감싸는 base64가 33% 손해라는 점이 지적됨. base45(RFC 9285)로 바꾸면 QR '영숫자 모드'를 쓸 수 있어 이득이 있을 것으로 보고, `airgap/channels/base45.py`를 직접 구현(라이브러리 없음, CLAUDE.md 규칙 2)한 뒤 `screen_qr.py`의 감싸기 방식을 교체
- **결과:** RFC 9285 부록의 표준 테스트 벡터 4개(`AB`→`BB8` 등)와 정확히 일치, 0~299바이트 무작위 왕복 300건 통과. 프레임을 실제 QR로 만들어 재보니 오류정정 M 기준 모듈 수가 조각 16B에서 29→25, 64B에서 41→37, 192B에서 65→57로 줄었다(변 기준 10~14% 감소, 같은 화면 면적 대비 1.2~1.35배). 예전 base64 원본을 못 읽게 되면 곤란하므로(규칙 3) 복조는 base45를 먼저 시도하고 실패하면 base64로 물러서게 했고, 이걸 회귀 테스트로 고정함
- **해석:** 문자 수만 보면 base45(3/2배)가 base64(4/3배)보다 오히려 많은데, QR 안에서 차지하는 비트는 더 적다 — 영숫자 모드가 문자 하나를 5.5비트에 담기 때문(base45 8.25비트/바이트 vs base64 10.67비트/바이트). **전송률을 올리는 다른 방법들(격자, 표시 시간 단축, 오류정정 하향)이 하나같이 광학적 여유를 깎아먹는 것과 달리, 이건 QR을 작게 만들어 오히려 인식에 유리하다.** 그래서 여러 개선안 중 이것을 가장 먼저 적용했다
- **다음:** 모듈 수 감소가 실제 카메라 인식률에도 이득으로 나타나는지는 실기기 측정이 있어야 안다. 같은 거리·같은 메시지로 이전 base64 조건과 비교할 수 있으면 좋지만, 실기기 측정 자체가 아직 없어서 base64 시절 기준선이 없다는 점은 한계다

## 2026-09-03 base45로 바꾸자 0 채움 구간에서 QR 생성이 죽음 — 스크램블링 도입

- **시도:** base45 교체 후 전체 테스트를 돌렸더니 `test_screen_qr_demo.py`의 파일 크기 페이로드 테스트가 `ValueError: glog(0)`으로 실패. 처음에는 특이한 입력에서만 나는 예외인 줄 알고 무작위 페이로드 200건으로 재현을 시도했으나 0건 실패, 대신 페이로드가 전부 0인 프레임에서 100% 재현됨
- **결과:** 원인은 `qrcode` 라이브러리가 오류정정 블록 하나가 통째로 0인 데이터를 만나면 내부 다항식 계산에서 죽는 것이었다. base45는 0 바이트를 '0' 문자로 적고, 영숫자 모드에서 그건 값이 0인 데이터 바이트가 되므로 이 조건을 밟는다(base64는 0 바이트가 'A'가 되므로 밟지 않아 지금까지 드러나지 않았다). **드문 경우가 아니었다** — `split_into_chunks`가 원본 길이를 맞추려고 마지막 조각을 0으로 채우므로, 파일 크기가 조각 크기의 배수가 아닌 한 거의 항상 생긴다. 프레임 바이트열에 고정 난수열을 XOR하는 스크램블링(`screen_qr.py`의 `_scramble`)을 넣어 해결. 전체 테스트 128개 통과
- **해석:** 실제 통신 규격(블루투스, 지상파 디지털 방송)이 같은 이유로 같은 처리를 한다 — 같은 값이 길게 이어지면 수신측 동기화나 인코더가 무너지는 문제를 미리 흩어놓는 것이다. **우리가 밟은 것은 그 문제의 아주 구체적인 사례**라, 발표에서 "왜 굳이 뒤섞어 보내는가"를 설명할 재료가 된다. 한편 이 버그가 base64 시절에는 우연히 가려져 있었다는 점도 기록해둘 만하다 — 감싸는 방식을 바꾸자 원래 있던 취약점이 드러난 것이지 base45가 만든 문제가 아니다
- **다음:** 스크램블 난수열은 고정 선형 합동 생성기(x73+41, 시작값 0x5A)다. 송수신이 같은 값을 쓰기만 하면 되므로 지금은 상수로 박아뒀는데, 설정으로 뺄지는 필요해지면 그때 판단

## 2026-09-03 QR 격자(한 화면에 여러 장)를 축 A 실험 변인으로 추가

- **시도:** "공간 축을 더 쓰면 전송률이 오르는가"를 같은 하드웨어에서 직접 보이기 위해, 한 화면에 QR을 n x n으로 띄우는 조건을 추가(`airgap/experiments/qr_grid.py`, `screen_qr_send.py --grid`). 기본값은 1(대조군)
- **결과:** 수신측은 **이미지를 격자대로 잘라 나눌 필요가 없었다.** zbar가 한 이미지 안의 QR 심볼을 전부 찾아주기 때문에, `ScreenQr.demodulate_all()`로 찾은 것을 모두 방울로 넣기만 하면 된다. 합성 이미지로 1x1·2x2·3x3을 검증해 방울 1/4/9개가 하나도 빠짐없이 복원됨. `screen_qr_recv.py`는 `--grid`를 기록용으로만 받고 판정 동작은 격자 수와 무관하다
- **해석:** 처음 계획은 수신 화면을 4등분해 각각 복조하는 것이었는데, 그 방식은 화면이 카메라 화각의 어디에 어떻게 걸렸는지에 의존한다 — 실기기에서 화면이 조금만 기울거나 화각 가운데에 안 오면 자른 경계가 QR을 갈라버린다. zbar에 통째로 넘기는 방식은 그런 가정이 아예 없어서 더 튼튼하고 코드도 짧다. **격자는 공짜가 아니다** — n을 올리면 QR 한 장의 화면상 크기가 1/n로 줄어드니, 이 실험이 재는 것은 "n^2배 빨라진다"가 아니라 "몇 배까지 가다가 어디서 꺾이는가"다
- **다음:** 격자를 기본값으로 켜면 밝기 변조와의 비교가 "공간 축을 쓰느냐"가 아니라 "QR만 최적화했느냐"가 되어 규칙 5에 어긋나므로, **M3 완료 조건인 두 채널 비교는 반드시 --grid 1로 잰다.** 격자 스윕(1/2/3)은 그 뒤 M4의 축 A 조건으로 따로 돌린다. 어느 n에서 인식이 무너지는지는 실기기 측정 전에는 알 수 없다

## 2026-09-04 화면 채널에도 파일 경유 경로 추가 — 안드로이드 두 대만으로 측정 가능

- **시도:** 실측 절차를 정리하다가, 음향 채널에는 스피커를 쓰지 않고 WAV를 만들어 폰으로 중계하는 경로(`acoustic_send.py --out-dir`)가 있는데 **화면 채널에는 그에 해당하는 것이 없다**는 것을 발견. 노트북 화면 없이는 가시광 채널을 아예 잴 수 없는 상태였다. 같은 방식으로 송신 화면을 동영상 파일로 써내는 `airgap/experiments/screen_video.py`를 만들고 `screen_qr_send.py`·`screen_flicker_send.py`에 `--out-dir`을 붙였다
- **결과:** 만든 동영상을 그대로 수신 스크립트에 넘겨 QR 2/2, 밝기 변조 2/2 판정 성공. 2x2 격자도 한 프레임에서 QR 4장이 모두 읽혔다. 동영상은 항상 30fps로 만들고 화면 하나를 `display_ms`에 해당하는 횟수만큼 반복해 넣는다 — 2fps 같은 낮은 프레임률로 파일을 만들면 폰 재생기가 제대로 다루지 못하는 경우가 있기 때문이다. 화면마다 QR 크기가 달라도 동영상 프레임 크기는 하나로 고정한다(재생 중 그림 크기가 바뀌면 카메라가 초점을 계속 다시 잡는다). 전체 테스트 134개 통과
- **해석:** 이로써 **세 채널이 모두 같은 모양의 경로를 갖게 됐다** — 노트북이 파일을 만들고, 폰 A가 재생하고, 폰 B가 받고, 노트북이 판정한다. 채널마다 측정 방식이 다르면 그 자체가 공정 비교를 흔들기 때문에(규칙 5) 구조가 같아진 것은 부수 효과가 아니라 개선이다. 원래 오디오 드라이버 고장에 대응하려고 만든 우회로가 결과적으로 세 채널 공통의 표준 경로가 된 셈이다
- **다음:** 폰 재생기가 동영상의 프레임률을 그대로 지키는지는 실기기에서 확인해야 한다. 재생기가 프레임을 건너뛰거나 보간하면 표시 시간이 달라져 전송률 계산이 어긋난다. 같은 조건을 노트북 화면 경로로도 재서 두 결과가 일치하는지 대조하는 것이 검증 방법이다

## 2026-09-05 실기기 본 측정 — 음향 0/10, 밝기 변조 0/10, QR 10/10

- **시도:** 파일 경유 경로로 세 채널을 각각 10회 측정. 음향 1m, 가시광 30cm, 메시지 31B 동일
- **결과:** QR 10/10(K=1). 밝기 변조 0/10 — **10회 전부 CRC 불일치, 프리앰블 미검출 0회**. 음향 0/10 — CRC 불일치 6, 프리앰블 미검출 4. 8월 10cm 음향은 미검출 5·CRC 1이었으므로 거리가 10배 멀어졌는데 미검출은 줄고 CRC가 늘었다
- **해석:** 밝기 변조의 "프리앰블은 매번 잡히는데 비트는 매번 틀림"은 무작위 잡음이 아니라 계통 원인이다. 아래 두 항목에서 원인을 코드에서 찾았다. QR의 `frames_seen` 35~65는 폰 녹화 시작 후 Enter를 누르기까지의 지연(1~2초)이지 인식 실패 횟수가 아니다 — 처음에 "인식률 2%"로 잘못 읽었다가 절차를 되짚어 바로잡음. **원본 파일(src 폴더)을 지우고 CSV만 남긴 것이 진단을 어렵게 했다.** 앞으로 원본은 반드시 보존한다
- **다음:** 아래 수정을 적용한 뒤 각 채널 1회씩만 재녹화하고 원본을 `diagnose.py`에 넣어 원인을 확정한다

## 2026-09-05 밝기 변조 실패 원인 — 양수 신호의 상관과 고정 기준값

- **시도:** 실패 패턴을 재현하려고 카메라 자동 노출(진폭 압축 + 느린 밝기 변동)을 시뮬레이션. 예전 복조기는 **잡음 σ=0.02만 얹어도 0/20**이었다
- **결과:** 결함 두 개가 겹쳐 있었다. ① 프리앰블 검출이 `np.correlate(밝기, 프리앰블)`을 그대로 썼는데, 밝기와 프리앰블 파형이 **둘 다 양수**라 패턴이 맞는 곳이 아니라 그냥 제일 밝은 구간에서 최댓값이 난다. 루프백에서는 밝기가 정확히 두 값뿐이라 우연히 맞았다. ② 판정 기준이 상수 0.5라 자동 노출이 진폭을 누르면 판정이 무너진다. 고친 것: 평균을 빼고 양쪽 에너지로 나눈 정규화 상호상관(NCC)으로 시작점을 찾고(임계값 0.3→0.8, 잡음만으로도 0.75까지 나오므로), 판정 기준은 3초 창의 **위아래 포락선 중간값**을 국소적으로 따라간다. 이동 중앙값·평균은 두 값뿐인 신호에서 비트 밀도에 끌려가므로 쓰면 안 된다(실제로 그렇게 했다가 전부 깨졌다). 진폭 20%·드리프트 0.15에서 20/20, 진폭 15%·드리프트 0.20에서도 20/20
- **해석:** 세 채널 중 밝기 변조만 판정이 절대적이었다는 보고서 6.3절의 분석이 맞았고, 그 뿌리는 이 두 줄의 코드였다. 소프트웨어 루프백이 완벽했던 이유가 곧 실기기에서 무너진 이유다 — **루프백은 신호가 정확한 값으로 오기 때문에 절대 판정과 상대 판정을 구별하지 못한다**
- **다음:** 대가가 하나 있다. 포락선 창(3초)보다 긴 연속 동일 비트 구간에서는 한 준위가 사라져 기준이 무너진다. 이 채널로 파일(0 채움 있음)을 보내지 않으므로 실측에는 영향 없음. 필요해지면 QR처럼 스크램블을 넣으면 된다

## 2026-09-05 음향 실패 원인 — 진폭 의존 검출과 기기 간 시계 차이

- **시도:** 잔향(반사 5개, RT 120ms)·진폭 0.3배·SNR 20dB를 시뮬레이션했더니 예전 검출기는 **진폭 0.3배에서 미검출 7~10/10**. 8월의 "미검출 5회"가 재현됐다. 그런데 잔향은 CRC 실패를 만들지 않았다. 9월의 "CRC 6회"를 만든 것은 **재생·녹음 기기의 시계 차이**였다 — 0.2%(2000ppm) 오차만으로 프리앰블은 잡히고 CRC는 5/5 실패
- **결과:** 세 가지를 바꿨다. ① 프리앰블 검출을 파형 상관 대신 **두 톤 에너지 비율 (E1−E0)/(E1+E0)의 시계열**과 ±1 패턴의 상관으로 — 위상·진폭에 무관. 1ms 걸음으로 거친 탐색 후 표본 단위 미세 탐색. 창 시작 위치와 심볼 가운데의 반 심볼 차이를 앞 패딩으로 맞추지 않으면 시작점이 정확히 반 심볼 밀린다(처음에 그 실수를 했다). ② 수신기가 **심볼 주기를 ±1.5% 범위에서 0.01% 걸음으로 추정** — 판정 또렷함 |E1−E0|/(E1+E0)의 평균이 최대인 주기를 고른다. CRC를 보지 않고 신호만으로 정하므로 계층을 넘지 않는다. ③ 심볼 경계를 빼고 **가운데 60%만** 판정에 사용(잔향 대응). 복합 조건(진폭 0.1·잔향 200ms·SNR 10dB·시계 0.5%)에서 10/10
- **해석:** 16비트 프리앰블은 시계 오차의 누적이 작아 살아남고, 그 뒤 300비트를 지나며 밀림이 반 칸을 넘어 CRC가 깨진다 — 9월 관찰과 정확히 같다. 0.2%는 폰 재생기가 44.1kHz를 48kHz로 리샘플링할 때 흔히 생기는 수준이다. **실기기 원본이 있으면 `diagnose.py`가 시계 오차를 %로 찍어 주므로 이 가설은 확정 가능하다**
- **다음:** 이 두 가설(자동 노출·시계 차이)은 시뮬레이션 재현일 뿐 실기기 확정은 아니다. 각 채널 1회 재녹화 → `diagnose.py` → 확정

## 2026-09-05 진단 도구와 QR 파일 전송

- **시도:** `airgap/experiments/diagnose.py` — wav나 mp4 하나를 넣으면 진폭·프리앰블 점수·추정 시계 오차·판정 또렷함(앞 1/4 vs 뒤 1/4)·포락선 폭·드리프트 폭을 표로 찍고 그림을 저장. 판정 코드와 같은 함수를 쓴다. `screen_qr_send/recv --file --chunk-size` — 이미지 같은 파일을 안드로이드 두 대 경로로 보낼 수 있게 함
- **결과:** 7,278B PNG를 조각 192B·격자 2×2로 → K=38, 방울 64개(오버헤드 1.68)로 복원, 동영상 226프레임(7.5초). 프레임당 QR 4장 전부 인식(합성 동영상). 실기기 인식률은 미측정
- **해석:** 이론 전송률은 192B×4장×2화면/초 = 1.5KB/s, 오버헤드 1.8배를 나누면 **실효 약 0.8KB/s**. 10KB 이미지에 12초. `--display-ms 200`이면 3배 빨라지지만 카메라가 버티는지는 실기기로만 안다. **영상(MB 단위)은 현실적으로 불가** — 1MB에 20분 이상. 보고서에 "이미지 크기까지"로 명시할 것
- **다음:** 실기기에서 `--file` 측정. 격자 2×2·화면 500ms부터 시작해 200ms로 줄여 가며 어디서 꺾이는지

<!-- 아래에 기록을 추가한다 -->
