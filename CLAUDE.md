# CLAUDE.md

Claude Code가 매 세션 시작 시 읽는 프로젝트 상수.
작업 전에 `PLAN.md`(현재 단계)와 `docs/ARCHITECTURE.md`(설계 규약)를 함께 확인한다.

---

## 프로젝트

**이름:** Air-Gap
**위치:** `~\Desktop\project` (Windows)

**한 줄 정의:** 인터넷을 쓰지 않고 정보를 전달하는 여러 물리 채널을 동일한 소프트웨어 구조 위에 구현하고, 전송 성능과 신호 선별의 한계를 공정하게 비교한다.

**연구 질문:**
> 인터넷을 사용하지 않고 정보를 전달할 때, 채널의 물리적 성질은 전송 한계와 신호 선별의 한계를 어떻게 결정하는가?

**두 축:**
- **축 A · 전송** — 정보를 얼마나 빠르게 보낼 수 있는가
- **축 B · 선별** — 원하는 신호만 어떻게 골라낼 것인가

두 축은 같은 물리적 제약(시간·주파수 맞바꿈)에 묶여 있다. 이것이 이 프로젝트의 지적 중심이다.

**맥락:** 고등학교 2학년 3인 팀의 교내 학술제(지식 나눔 학술제 '광장') 출품작. 우수 모둠 선정 시 경신고와의 공동 학술제 학교 대표로 출전.
**최종 산출물은 코드가 아니라 보고서와 현장 시연이다.** 코드는 그 둘을 뒷받침하는 수단이다.

---

## 절대 규칙

### 1. 학생이 이해하지 못하는 코드는 실패한 코드다

제품이 아니라 학습 결과물이다. 발표장에서 "이 부분이 어떻게 동작하나요"에 답하지 못하면, 코드가 아무리 잘 돌아도 실패다.

- 함수는 짧게, 한 가지 일만
- 신호처리 코드에는 **무엇을 하는지가 아니라 왜 하는지**를 주석으로 남긴다
- 영리한 한 줄보다 투박한 다섯 줄이 낫다
- 새 개념(FFT, LT 부호, STFT 등)을 처음 도입할 때 모듈 상단 docstring에 3~5줄 개념 설명

### 2. 핵심 알고리즘은 라이브러리로 때우지 않는다

**직접 구현:** FSK 변복조 / LT 파운틴 부호 / CRC·프레임 동기화·프리앰블 검출 / BER·처리율 계산 / 선별 필터 판정 로직

**라이브러리 허용:** FFT(`numpy.fft`), 오디오 입출력(`sounddevice`), 카메라(`opencv-python`), QR 심볼 생성·인식, 그래프(`matplotlib`), 통계(`scipy.stats`)

기준: **탐구의 대상이면 직접 구현하고, 도구면 가져다 쓴다.** 애매하면 직접 구현 쪽으로.

### 3. 측정 데이터는 손대지 않는다

- `data\raw\` 아래는 생성 후 읽기 전용. 수정도 삭제도 금지
- 실험이 잘못됐으면 새 실행 ID로 재측정한다. 덮어쓰지 않는다
- 이상치 제거는 분석 단계에서만, 기준을 코드에 명시하고 원본은 보존

### 4. 실패한 결과도 결과다

작동하지 않은 시도는 `FINDINGS.md`에 기록한다. 무엇을 시도했고, 어떤 결과가 나왔고, 왜 안 됐다고 보는지.

**성능이 안 나왔다고 조건을 유리하게 바꾸지 않는다.** 안 나온 그대로 보고하고 원인을 분석한다.

### 5. 채널 간 비교는 공정해야 한다

모든 채널이 동일한 상위층(패킷 포맷, CRC, 파운틴 부호, 측정 코드)을 공유한다. 특정 채널만 최적화하거나 특정 채널에만 오류정정을 넣는 것은 금지. 비교 가능성이 이 연구의 방법론적 핵심이다.

---

## 환경 (Windows)

- **Python 3.11** 고정, 타입 힌트 사용
- 가상환경: `.venv`

```powershell
cd ~\Desktop\project
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1     # 실행 정책 오류 시: Set-ExecutionPolicy -Scope Process RemoteSigned
pip install -r requirements.txt
```

```
numpy scipy sounddevice soundfile opencv-python qrcode[pil] pyzbar matplotlib pandas pyyaml pytest ruff
```

**Windows 주의사항:**
- 경로는 `pathlib.Path`로만 다룬다. 문자열 `\` 하드코딩 금지
- `sounddevice`가 장치를 못 찾으면 `python -m sounddevice`로 장치 목록을 먼저 확인한다. WASAPI/MME 등 호스트 API에 따라 인덱스가 다르다
- 마이크 접근이 차단되면 설정 → 개인 정보 → 마이크에서 데스크톱 앱 허용
- 파일 인코딩은 전부 UTF-8. `open()`에 `encoding='utf-8'` 명시 (Windows 기본은 cp949라 한글이 깨진다)
- 콘솔 한글 깨짐: `chcp 65001`

---

## 디렉터리

```
project\
  airgap\
    core\          채널과 무관한 공통층
    channels\      채널별 물리층 (축 A)
    selective\     선별 필터 (축 B)
    experiments\   측정 자동화 및 분석
    demo\          발표 시연용
  config\          YAML 파라미터
  data\
    raw\           측정 원본 (읽기 전용)
    results\       분석 산출물, 그래프
  docs\            설계 문서, 보고서 초안
  tests\
```

**의존 방향은 한쪽으로만 흐른다.** `channels\` → `core\`는 되지만 `core\`가 `channels\`를 import하면 안 된다. core는 어떤 채널이 존재하는지 몰라야 한다.

---

## 코딩 규약

- 물리 상수와 실험 파라미터는 코드에 박지 말고 `config\`의 YAML로 뺀다
- **단위를 변수명에 넣는다:** `symbol_duration_ms`, `distance_cm`, `freq_hz`. 단위 없는 숫자 변수는 버그의 씨앗
- 난수를 쓰는 곳은 반드시 시드를 받고 기록한다. 재현 불가능한 실험은 실험이 아니다
- `print` 대신 `logging`. 실험 실행 시 전체 설정을 로그 첫 줄에 덤프
- 포매터 `ruff format`, 린터 `ruff check`

---

## 커밋 규약

- 한 커밋은 한 가지 변경. 여러 채널을 동시에 건드리지 않는다
- 메시지는 한국어 명령형: `음향 채널 프리앰블 검출 임계값 조정`
- 접두사: `feat:` `fix:` `exp:`(측정 실행) `docs:` `refactor:`
- `exp:` 커밋은 메시지에 실행 ID 포함

---

## Claude Code에게

**작업 전**
1. `PLAN.md`에서 현재 단계 확인
2. 그 단계의 완료 조건을 먼저 읽는다
3. **앞질러 가지 않는다.** 일정이 촉박하므로 동작하는 최소 버전 확보가 항상 우선

**작업 중**
- 설계 판단이 갈리면 임의로 정하지 말고 선택지와 근거를 제시하고 멈춘다
- 물리적 검증이 필요한 값(실제 몇 미터까지 잡히는지 등)은 추측하지 말고 측정 스크립트를 만들어 학생이 재도록 한다
- 하드웨어 없이 검증되도록 모든 채널은 **루프백 모드**를 지원해야 한다

**작업 후**
- 새 기능에는 테스트를 붙인다. 최소한 루프백 왕복 테스트는 반드시
- `PLAN.md` 체크박스 갱신
- 예상과 달랐던 점은 `FINDINGS.md`에 한 줄이라도

**하지 말 것**
- 요청하지 않은 리팩터링
- 성능 최적화 (이 프로젝트에서 속도는 측정 대상이지 목표가 아니다)
- 보고서 문장을 코드 주석에 쓰기

---

## 절대 마감

| 날짜 | 마감 |
|---|---|
| 2026. 8. 14. (금) | 활동 계획서 (제출 완료) |
| 2026. 8. 28. (금) | 중간 보고서 |
| 2026. 9. 10. (목) | 최종 보고서 및 발표 |

**남은 실작업 약 4주. 완성도보다 완료를 우선한다.**
