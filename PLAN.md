# PLAN.md

작업 순서와 완료 조건. Claude Code는 세션 시작 시 여기서 **현재 단계**를 확인하고 그 단계의 완료 조건을 먼저 읽는다.

**현재 단계: M1**

---

## 원칙

**앞질러 가지 않는다.** 남은 실작업은 약 4주. 여러 단계를 동시에 진행하면 어느 것도 끝나지 않는다.

**M2까지가 안전선이다.** M2가 끝나면 그것만으로 보고서와 발표가 성립한다. M3 이후는 전부 보너스이며 실패해도 프로젝트는 무너지지 않는다. 이 순서는 의도된 것이다.

**버리는 순서가 정해져 있다.** 일정이 밀리면 확장 채널 → 축 B의 B-2 → 축 B의 B-1 순으로 버린다. M5(발표 준비)는 절대 줄이지 않는다.

---

## M0 — 뼈대 (8/14 ~ 8/16)

디렉터리, 의존성, 공통층 골격. 채널은 아직 없다.

- [x] 디렉터리 생성, `requirements.txt`, `.venv`, `ruff` 설정
- [x] `core/bits.py` — 바이트 ↔ 비트 배열
- [x] `core/frame.py` — 프리앰블·시드·길이·CRC16 프레임 조립과 해체
- [x] `core/channel.py` — `Channel` 추상 클래스, `ChannelCaps`
- [x] `core/metrics.py` — BER, 실효 전송률, 유실률 계산
- [x] `tests/` — 위 모듈 단위 테스트

**완료 조건:** 임의 바이트열 → 프레임 → 원본 왕복 테스트 통과. 비트를 인위적으로 뒤집으면 CRC가 반드시 잡아낸다.

---

## M1 — 음향 채널 (8/16 ~ 8/21) ★ 최소 성공선

**가장 중요한 단계.** 이게 끝나면 최악의 경우에도 발표할 것이 생긴다.

- [x] `channels/acoustic_fsk.py` — 두 반송 주파수로 0/1 대응, 심볼 단위 파형 생성
- [x] 복조 — 심볼 구간별 FFT, 두 주파수 성분 크기 비교로 비트 판정
- [x] 프리앰블 상관 검출로 수신 시작 지점 탐색
- [x] 루프백 테스트 (잡음 없음 → 무오류, SNR 낮춤 → 오류율 상승)
- [x] WAV 파일 경유 테스트
- [ ] 실제 스피커 → 마이크 전송 성공 — **막힘: 노트북 오디오 드라이버 고장으로 라이브 경로 실행 불가** (FINDINGS.md 2026-08-20 참고)
- [ ] 안드로이드 두 대 + 파일 경유 경로로 대체 실측 — `acoustic_send.py --out-dir` / `acoustic_recv.py --in-dir` 준비 완료, 실기기 실행은 학생이 해야 함 (아래 참고)

**완료 조건:** 1m 거리에서 20자 내외 문자열을 무오류 복원. 10회 중 8회 이상 성공.
경로는 상관없다 — 노트북 스피커→마이크 직접 녹음이든, 안드로이드 두 대(폰 A 재생 → 폰 B 녹음 → WAV로 노트북에 전달)든 같은 판정 기준(`_evaluate`, acoustic_recv.py)을 쓴다.
**노트북 드라이버가 고쳐지면** 라이브 경로(`acoustic_send.py`/`acoustic_recv.py`, `--out-dir`/`--in-dir` 없이)로도 같은 실측을 한 번 더 돌려 두 경로의 성공률을 비교한다 (two-way 확인).

**막혔을 때 점검 순서:** 루프백은 되는가 → 파일 경유는 되는가 → 프리앰블이 잡히는가 → 표본율이 송수신 양쪽에서 같은가 → 심볼 길이가 주파수 분해능에 비해 너무 짧지 않은가 → (라이브 경로) `sounddevice` 장치 인덱스가 맞는가 / (파일 경유 경로) 폰 녹음이 WAV인가, ffmpeg 변환이 필요한가

---

## M2 — 파운틴 부호 + 중간 보고서 (8/21 ~ 8/28)

- [ ] `core/fountain.py` — LT 인코더 (로버스트 솔리톤 분포로 차수 추출, 시드 기반 조각 선택)
- [ ] LT 디코더 (차수 1 조각 확정 → XOR 소거 연쇄)
- [ ] 조각 유실률을 인위 주입해 복원 오버헤드 측정
- [ ] 음향 채널 거리별 1차 측정 (1m, 2m, 3m, 5m)
- [ ] **중간 보고서 작성 및 제출 (8/28 마감)**

**완료 조건:** 조각의 30%를 무작위로 버려도 재전송 없이 복원된다. 거리별 BER 그래프 1장.

**중간 보고서 내용:** 구현 완료 상태, 거리별 기초 데이터, 남은 일정. 완성된 결론은 필요 없다.

---

## M3 — 가시광 채널 (8/28 ~ 9/3)

**밝기 변조를 먼저 만든다.** 훨씬 쉽고, QR이 실패해도 가시광 채널 하나는 남는다.

- [ ] `channels/screen_flicker.py` — 화면 전체 밝기 변조, 카메라 또는 조도 측정으로 수신
- [ ] `channels/screen_qr.py` — 조각을 QR 심볼로 표시, 카메라 캡처 후 인식
- [ ] 두 채널 전송률 비교 측정

**완료 조건:** 화면에서 카메라로 이미지 파일 1개 복원. QR과 밝기 변조의 전송률 차이를 수치로 제시.

**두 채널을 짝으로 두는 이유:** 매질과 하드웨어가 완전히 동일하고 **부호화 차원만 다르다.** 성능 차이가 온전히 "공간 차원을 쓰느냐"에서 온다는 것을 통제된 조건에서 보일 수 있다.

---

## M4 — 축 A 본 실험 (9/3 ~ 9/7)

- [ ] `experiments/run_matrix.py` — 조건 조합 자동 순회, 반복 측정
- [ ] 음향: 거리 × 심볼 길이 × 배경 소음 × 주파수 대역
- [ ] 가시광: 거리 × 각도 × 화면 갱신률
- [ ] 블루투스 기준선 실측 (구현 대상 아님, 파일 전송 속도만)
- [ ] 차폐 실험 — 벽, 문, 종이, 알루미늄 포일, 각도별 수신 가능 여부
- [ ] `experiments/analyze.py` — 통계 처리와 그래프 생성

**완료 조건:** 조건별 최소 20회 데이터가 `data/raw/`에 쌓이고, 채널 비교표와 주요 그래프가 자동 생성된다.

---

## M4.5 — 축 B 선별 실험 (9/3 ~ 9/7, M4와 병행)

축 A 코드(FFT, 카메라)가 완성돼야 재사용이 되므로 M3 이후에 시작한다.

### B-1. 선택적 소음 제거 (음향 담당) ★ 축 B의 우선순위 1위

- [ ] `selective/band_filter.py` — 주파수 대역 기반 통과 판정
- [ ] `selective/pattern_filter.py` — 주파수가 규칙적으로 오르내리는 시간 패턴 검출
- [ ] 분석 창 길이를 변인으로 **잡음 감쇠량(dB)** 과 **검출 지연(ms)** 측정
- [ ] 두 방식의 오검출률 비교 (같은 대역의 방해음 조건)
- [ ] (여유 시) 마이크 2개 도달 시차로 방향 추정 → 주파수 필터가 실패하는 조건에서 방향 필터가 성공하는지 확인

**완료 조건:** 창 길이에 따른 감쇠량–지연 곡선 1장. 두 판정 방식의 오검출률 비교표.

**물리 연계:** 창 길이 Δt와 주파수 분해능 Δf 사이의 맞바꿈. **M1에서 심볼 길이를 줄였을 때 오류율이 오른 것과 같은 제약이다.** 두 그래프를 같은 축에 겹치는 것이 M5의 핵심 결과.

**생명 연계 (문헌):** 달팽이관의 병렬 주파수 분석 구조, 양이 시차를 이용한 공간 필터링(칵테일 파티 효과). 기계는 주파수로 거르고 사람은 방향으로 거른다.

### B-2. 감각의 틈 (가시광 담당)

- [ ] 화면 깜빡임 주파수를 올려가며 사람의 인지 한계 측정 (팀원 대상, 주관 보고)
- [ ] 같은 조건에서 카메라의 복원 한계 측정
- [ ] 두 한계 사이 구간의 존재 여부 확인
- [ ] (도전) 카메라 순차 주사(롤링 셔터) 특성으로 프레임률보다 빠른 신호 복원

**완료 조건:** 인지 한계와 기계 한계를 같은 축에 표시한 그래프 1장.

**물리 연계:** 롤링 셔터. 시간 정보가 공간 좌표로 변환되는 구조라 QR의 공간 부호화와 개념적으로 이어진다.

**생명 연계 (문헌):** 임계 융합 주파수의 종간 차이. 감각의 시간 분해능이 곧 그 종의 정보 대역폭.

---

## M5 — 통합과 발표 (9/7 ~ 9/10)

- [ ] 섀넌 채널 용량 이론값 계산, 실측치와 비교
- [ ] **축 A와 축 B 곡선을 같은 축에 겹쳐 동일 제약임을 보이기** ← 발표의 절정
- [ ] 생물학적 대조 정리 (문헌 기반)
- [ ] `demo/` — 시연 스크립트 (실험 코드와 분리, 실패해도 우아하게)
- [ ] 최종 보고서
- [ ] 발표 자료

**완료 조건:** 인터넷 없는 노트북 두 대로 시연 성공. 리허설 최소 2회.

**시연 설계 원칙:** 실시간 측정에 의존하지 않는다. 실패해도 미리 녹화한 영상으로 이어갈 수 있게 준비한다. 발표장은 조명도 소음도 통제되지 않는다.

---

## 확장 과제 (M4가 끝나고 시간이 남을 때만)

**하나라도 M2~M5를 지연시키면 즉시 중단한다.**

**E1 — 근초음파 (난이도 하, 우선)**
음향 채널의 반송 주파수를 18~20kHz로 올린 변형. M1 코드에서 설정값만 바꾸면 되므로 비용 대비 효과가 가장 좋다. 성능은 가청 대역보다 나쁘지만 **사람이 신호의 존재를 알아채지 못한다** — 축 B의 B-2와 같은 논지를 소리에서 반복하는 셈이라 결론이 두꺼워진다.

**E2 — 진동 채널 (난이도 중)**
폰 진동모터로 송신, 다른 기기 가속도계로 수신. 음향과 같은 종파인데 매질이 고체다. **같은 파동 종류 안에서 매질만 바꾼 통제 비교**가 되는 것이 가치. 접촉 방식이라 거리 변인 설계를 다시 해야 한다.

**E3 — 자기장 채널 (난이도 상, 도전)**
코일에 저주파 신호를 흘려 송신, 폰 자기계로 수신. 전송률은 초당 수 비트로 매우 낮지만 **종이와 벽을 통과한다.** 소리와 빛이 차단되는 조건에서 유일하게 살아남는 채널이라는 점이 발표의 결정적 장면. 폰 자기계 표본율이 낮아 성공을 보장할 수 없다. **실패하면 실패했다고 기록하고 넘어간다.**

---

## 위험 요소

| 위험 | 대응 |
|---|---|
| 카메라 프레임 동기화 실패 | M3의 QR을 포기하고 밝기 변조만. 그것도 안 되면 음향 단독으로 결론까지 |
| 마이크·스피커 기기별 특성 차이 | 측정 기기를 하나로 고정하고 기종을 기록. 기기 비교는 확장 과제로 |
| 발표장 소음으로 시연 실패 | 녹화 영상 백업. 근초음파 대신 가청 대역을 쓰면 성공률이 높다 |
| 팀원 진도 격차 | 채널이 독립 모듈이므로 한 명이 막혀도 나머지는 진행. 주 1회 통합 시점 고정 |
| 축 B가 축 A를 잠식 | 축 B는 M3 이후에만 착수. M2 이전에 손대지 않는다 |
| 일정 초과 | 확장 채널 → B-2 → B-1 순으로 버린다. M5는 줄이지 않는다 |
