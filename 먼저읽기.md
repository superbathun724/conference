# 먼저 읽기 (2026-09-04)

## 이 zip에 없는 것: `.venv`

가상환경 폴더(537MB)는 용량 때문에 뺐습니다. **그래서 폴더를 통째로
지우고 새로 넣으면 안 됩니다.** 아래 방법으로 하세요.

## 적용 방법 (권장)

이 zip의 내용을 **기존 `\~\\Desktop\\project` 위에 그대로 덮어쓰기**만 하면
됩니다. `.venv`는 건드리지 않으므로 그대로 살아 있습니다.

1. zip 압축 풀기
2. 나온 파일·폴더를 전부 선택해서 `\~\\Desktop\\project` 안에 끌어다 놓기
3. "대상에 이미 있는 파일 덮어쓰기" 선택

삭제된 파일은 없고 추가·수정만 있으므로, 덮어쓰기만으로 정확히 최신
상태가 됩니다. `data\\raw`의 기존 측정 원본과 `.git` 기록도 그대로 들어
있습니다.

### 새 폴더에 풀고 싶다면

기존 `project\\.venv` 폴더를 새 폴더 안으로 복사해 넣으세요. 그것만 있으면
됩니다.

## 적용 후 반드시 확인

```powershell
cd \~\\Desktop\\project
.\\.venv\\Scripts\\Activate.ps1
chcp 65001
pytest
```

**134개 통과**가 나와야 합니다. 숫자가 다르면 덮어쓰기가 덜 된 것이니
측정을 시작하지 말고 확인하세요.

## 그 다음

`산출물\\에어갭\_실측\_절차\_안내서.html`을 브라우저로 열고 0장부터 순서대로
진행하시면 됩니다. 11장 측정 기록지는 인쇄해서 쓰세요.

\---

## 이번에 달라진 것 요약

### 새 파일

|경로|내용|
|-|-|
|`airgap\\channels\\base45.py`|base45(RFC 9285) 부호화 — 같은 조각을 더 작은 QR에 담는다|
|`airgap\\experiments\\qr\_grid.py`|QR을 n×n 격자로 붙인다 (축 A 실험 변인)|
|`airgap\\experiments\\screen\_video.py`|송신 화면을 동영상으로 써낸다 — 안드로이드 2대 경로의 핵심|
|`tests\\test\_base45.py`|RFC 표준 벡터 4개 + 왕복 + 잘못된 입력 거부|
|`tests\\test\_qr\_grid.py`|1×1 / 2×2 / 3×3 격자 왕복|
|`tests\\test\_screen\_video.py`|동영상 출력·되읽기 왕복|

### 고친 파일

|경로|변경 내용|
|-|-|
|`airgap\\channels\\screen\_qr.py`|base64 → base45 교체, 스크램블 추가, `demodulate\_all()` 추가|
|`airgap\\experiments\\screen\_qr\_send.py`|`--grid n`(기본 1), `--out-dir`로 동영상 저장|
|`airgap\\experiments\\screen\_qr\_recv.py`|한 프레임의 QR을 모두 읽음, CSV에 `qr\_symbols\_seen`·`grid` 열 추가|
|`airgap\\experiments\\screen\_flicker\_send.py`|`--out-dir`로 동영상 저장|
|`tests\\test\_channels.py`|0 채움 구간 회귀 테스트, 예전 base64 원본 읽기 테스트|
|`FINDINGS.md`|09-03 항목 3개, 09-04 항목 1개 추가|
|`PLAN.md`|M3 완료 항목 3개 체크, M4 가시광 조건에 격자 수 추가|
|`guide.txt`|`--grid`·`--out-dir` 설명, CSV 새 열 설명|

### 건드리지 않은 것

`core\\` 전부, `channels\\acoustic\_fsk.py`, `channels\\screen\_flicker.py`,
`config\\` 전부, `demo\\` 전부, `data\\raw\\` 전부.

### 새 폴더 `산출물\\`

|파일|용도|
|-|-|
|`에어갭\_실측\_절차\_안내서.html`|**지금 보실 것.** 실측 절차 + 인쇄용 기록지|
|`에어갭\_최종보고서\_초안.docx`|제출용 보고서 초안 (실측 자리는 비어 있음)|
|`에어갭\_발표자료.html`|발표용 슬라이드 15장|

\---

## 측정 시작 전 준비물

* \[ ] `pytest` 134개 통과
* \[ ] 폰 2대 (또는 노트북 화면 + 폰 1대)
* \[ ] 줄자
* \[ ] 거치대 — 삼각대·책 더미·상자 무엇이든. **손으로 들지 않기**
* \[ ] 측정 기록지 인쇄 (안내서 11장)
* \[ ] 조용한 교실, 에어컨·환풍기 끄기

