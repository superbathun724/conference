# PLAN.md

작업 순서와 완료 조건. Claude Code는 세션 시작 시 여기서 **현재 단계**를 확인하고 그 단계의 완료 조건을 먼저 읽는다.

**현재 단계: M3** (사용자 승인으로 M2 완료 전 조기 착수 — M1 실기기 검증·M2 중간 보고서는 학생이 별도로 진행)

---

## 원칙

**앞질러 가지 않는다.** 남은 실작업은 약 4주. 여러 단계를 동시에 진행하면 어느 것도 끝나지 않는다.

**M2까지가 안전선이다.** M2가 끝나면 그것만으로 보고서와 발표가 성립한다. M3 이후는 전부 보너스이며 실패해도 프로젝트는 무너지지 않는다. 이 순서는 의도된 것이다.

**버리는 순서가 정해져 있다.** 일정이 밀리면 확장 채널 → 축 B의 B-2 → 축 B의 B-1 순으로 버린다. M5(발표 준비)는 절대 줄이지 않는다.

---

## M0 — 뼈대 (8/14 ~ 8/16)

디렉터리, 의존성, 공통층 골격. 채널은 아직 없다.

- [x] 디렉터리 생성, `requirements.txt`, `.venv`, `ruff` 설정
- [x] `core/bits.py` — 바이트 ↔ 비트 배열
- [x] `core/frame.py` — 프리앰블·시드·길이·CRC16 프레임 조립과 해체
- [x] `core/channel.py` — `Channel` 추상 클래스, `ChannelCaps`
- [x] `core/metrics.py` — BER, 실효 전송률, 유실률 계산
- [x] `tests/` — 위 모듈 단위 테스트

**완료 조건:** 임의 바이트열 → 프레임 → 원본 왕복 테스트 통과. 비트를 인위적으로 뒤집으면 CRC가 반드시 잡아낸다.

---

## M1 — 음향 채널 (8/16 ~ 8/21) ★ 최소 성공선

**가장 중요한 단계.** 이게 끝나면 최악의 경우에도 발표할 것이 생긴다.

- [x] `channels/acoustic_fsk.py` — 두 반송 주파수로 0/1 대응, 심볼 단위 파형 생성
- [x] 복조 — 심볼 구간별 FFT, 두 주파수 성분 크기 비교로 비트 판정
- [x] 프리앰블 상관 검출로 수신 시작 지점 탐색
- [x] 루프백 테스트 (잡음 없음 → 무오류, SNR 낮춤 → 오류율 상승)
- [x] WAV 파일 경유 테스트
- [ ] 실제 스피커 → 마이크 전송 성공 — **막힘: 노트북 오디오 드라이버 고장으로 라이브 경로 실행 불가** (FINDINGS.md 2026-08-20 참고)
- [ ] 안드로이드 두 대 + 파일 경유 경로로 대체 실측 — `acoustic_send.py --out-dir` / `acoustic_recv.py --in-dir` 준비 완료. 2026-08-21에 10cm 거리로 1차 실측(태블릿-폰), 4/10 성공(40%) — 완료 조건(1m, 8/10)에는 못 미치고 거리도 다름. 1m 재측정 필요 (FINDINGS.md 2026-08-21 참고)

**완료 조건:** 1m 거리에서 20자 내외 문자열을 무오류 복원. 10회 중 8회 이상 성공.
경로는 상관없다 — 노트북 스피커→마이크 직접 녹음이든, 안드로이드 두 대(폰 A 재생 → 폰 B 녹음 → WAV로 노트북에 전달)든 같은 판정 기준(`_evaluate`, acoustic_recv.py)을 쓴다.
**노트북 드라이버가 고쳐지면** 라이브 경로(`acoustic_send.py`/`acoustic_recv.py`, `--out-dir`/`--in-dir` 없이)로도 같은 실측을 한 번 더 돌려 두 경로의 성공률을 비교한다 (two-way 확인).

**막혔을 때 점검 순서:** 루프백은 되는가 → 파일 경유는 되는가 → 프리앰블이 잡히는가 → 표본율이 송수신 양쪽에서 같은가 → 심볼 길이가 주파수 분해능에 비해 너무 짧지 않은가 → (라이브 경로) `sounddevice` 장치 인덱스가 맞는가 / (파일 경유 경로) 폰 녹음이 WAV인가, ffmpeg 변환이 필요한가

---

## M2 — 파운틴 부호 + 중간 보고서 (8/21 ~ 8/28)

- [x] `core/fountain.py` — LT 인코더 (로버스트 솔리톤 분포로 차수 추출, 시드 기반 조각 선택)
- [x] LT 디코더 (차수 1 조각 확정 → XOR 소거 연쇄)
- [x] 조각 유실률을 인위 주입해 복원 오버헤드 측정 — `experiments/fountain_overhead.py`,
      K=40 기준 0~50% 유실 전부 복원 성공, 오버헤드 1.8~2.0배 (FINDINGS.md 2026-08-20 참고).
- [x] 후속 측정: K 스윕(100/200/500)·c 스윕(0.03/0.1/0.2/0.5) — K가 커질수록 오버헤드가
      1.8배→1.36배(K=500)로 감소, c는 단조적이지 않음(K=100에서 정수 반올림 경계 효과로 추정)
      (FINDINGS.md 2026-08-20 참고)
- [ ] 음향 채널 거리별 1차 측정 (1m, 2m, 3m, 5m) — 학생이 실기기로 진행 (M1 대체 경로 참고)
- [ ] **중간 보고서 작성 및 제출 (8/28 마감)** — 초안 `docs/INTERIM_REPORT.md` 작성함(2026-08-21).
      구현 현황·현재까지 측정 데이터(파운틴 오버헤드, 음향 10cm 예비 측정)·남은 일정 정리.
      제출 전 학생이 검토·보완 필요

**완료 조건:** 조각의 30%를 무작위로 버려도 재전송 없이 복원된다 — **달성.** 거리별 BER 그래프 1장(음향 실기기 측정 필요, 아직 미완료).

**중간 보고서 내용:** 구현 완료 상태, 거리별 기초 데이터, 남은 일정. 완성된 결론은 필요 없다.

---

## M3 — 가시광 채널 (8/28 ~ 9/3)

**밝기 변조를 먼저 만든다.** 훨씬 쉽고, QR이 실패해도 가시광 채널 하나는 남는다.

- [x] `channels/screen_flicker.py` — 화면 전체 밝기 변조(2준위), `Channel` 구현 완료, 루프백 테스트 통과.
      카메라/조도 센서 실기기 검증은 아직 — 실측 스크립트는 M3 다음 작업으로 미룸
- [x] `channels/screen_qr.py` — 조각을 QR 심볼로 표시, `Channel` 구현 완료, 루프백 테스트 통과.
      `qrcode`→`pyzbar` 왕복에서 0x80 이상 바이트가 깨지는 문제를 base64로 우회함(FINDINGS.md 2026-08-20 참고)
- [x] `experiments/fountain_over_qr.py` — LT 파운틴 방울을 실제 프레임·QR 이미지로 감싸 modulate→demodulate
      루프백으로 통과시키는 통합 실험(카메라 불필요). K=10 기준 유실률 0~30%에서 80/80 복원 성공
      (FINDINGS.md 2026-08-20 참고)
- [x] `experiments/screen_qr_send.py` / `screen_qr_recv.py` — M1의 acoustic_send/recv와 같은 패턴의
      실기기 측정 스크립트. 수신측은 두 경로 지원: (1) 웹캠 등으로 직접 촬영,
      (2) `--in-dir`로 안드로이드 폰이 찍은 동영상 파일 경유(M1 방법 B와 동일한 발상,
      cv2.VideoCapture가 파일 경로도 받아들여서 저비용으로 추가함). 합성 동영상으로
      경로 자체는 검증했지만 실제 카메라 실행은 아직 안 됨 — 학생이 재야 함(guide.txt 방법 D)
- [x] `screen_flicker`용 실기기 측정 스크립트(send/recv) — `screen_flicker_send.py`/`recv.py`.
      QR과 달리 파운틴 스트리밍이 아니라 음향 채널과 같은 방식(시행마다 프레임 하나,
      Enter로 동기화)이라 acoustic_send/recv 구조를 그대로 따름. 수신측은 D-1/D-2와
      같이 실시간 카메라·안드로이드 동영상 경유 두 경로 지원(guide.txt 방법 E).
      합성 동영상으로 왕복 검증(루프백 아님, 실제 mp4 인코딩·디코딩 경유)까지는
      확인, 실제 카메라 실행은 아직 안 됨 — 학생이 재야 함
- [x] QR 페이로드 감싸기를 base64 → base45(RFC 9285)로 교체 — 같은 조각이 더 작은 QR에 들어간다
      (모듈 수 조각 192B 기준 65→57). 0 채움 구간이 QR 생성기를 깨뜨리는 문제가 같이 드러나
      스크램블링으로 해결 (FINDINGS.md 2026-09-03 참고)
- [x] QR 격자(`--grid n`, 한 화면에 n x n장)를 축 A 실험 변인으로 추가 — 기본값 1이 대조군.
      수신측은 zbar가 한 이미지의 심볼을 전부 찾아주므로 화면을 잘라 나누지 않는다
      (`airgap/experiments/qr_grid.py`). 합성 이미지로 1x1/2x2/3x3 검증, 실기기는 아직
- [x] 화면 채널에도 파일 경유 경로 추가(`--out-dir`로 송신 동영상 생성) — 노트북 화면 없이
      안드로이드 두 대만으로 가시광 채널을 잴 수 있다. 세 채널의 측정 경로가 같은 모양이 됐다
      (`airgap/experiments/screen_video.py`, FINDINGS.md 2026-09-04)
- [x] 실기기 본 측정 1차 (09-05) — QR 10/10, 밝기 변조 0/10, 음향 0/10. 실패 원인을 코드에서 찾아 수정
      (밝기: 양수 상관·고정 기준값 / 음향: 진폭 의존 검출·시계 차이). FINDINGS.md 09-05 항목 참고
- [ ] **재측정 (수정 후)** — 각 채널 1회 재녹화 → `diagnose.py`로 원인 확정 → 10회 본 측정. 원본 보존할 것
- [ ] 두 채널 전송률 비교 측정 — 위 스크립트들의 실기기 실행 결과가 있어야 가능.
      **반드시 `--grid 1`(대조군)로 잰다** — 격자를 켜고 재면 "공간 축을 쓰느냐"가 아니라
      "QR만 최적화했느냐"의 비교가 되어 규칙 5에 어긋난다

**완료 조건:** 화면에서 카메라로 이미지 파일 1개 복원 — **소프트웨어 루프백으로는 확인, 실기기(카메라) 검증은 아직.**
QR과 밝기 변조의 전송률 차이를 수치로 제시 — 실기기 측정 후 가능.

**두 채널을 짝으로 두는 이유:** 매질과 하드웨어가 완전히 동일하고 **부호화 차원만 다르다.** 성능 차이가 온전히 "공간 차원을 쓰느냐"에서 온다는 것을 통제된 조건에서 보일 수 있다.

---

## M4 — 축 A 본 실험 (9/3 ~ 9/7)

- [ ] `experiments/run_matrix.py` — 조건 조합 자동 순회, 반복 측정
- [ ] 음향: 거리 × 심볼 길이 × 배경 소음 × 주파수 대역
- [ ] 가시광: 거리 × 각도 × 화면 갱신률 × **QR 격자 수(1/2/3)** — 격자는 같은 하드웨어에서
      공간 축만 더 쓰는 조건이라 축 A의 논지를 직접 보인다. 어디서 인식이 꺾이는지가 관전 포인트
- [ ] 블루투스 기준선 실측 (구현 대상 아님, 파일 전송 속도만)
- [ ] 차폐 실험 — 벽, 문, 종이, 알루미늄 포일, 각도별 수신 가능 여부
- [ ] `experiments/analyze.py` — 통계 처리와 그래프 생성

**완료 조건:** 조건별 최소 20회 데이터가 `data/raw/`에 쌓이고, 채널 비교표와 주요 그래프가 자동 생성된다.

---

## M4.5 — 축 B 선별 실험 (9/3 ~ 9/7, M4와 병행)

축 A 코드(FFT, 카메라)가 완성돼야 재사용이 되므로 M3 이후에 시작한다.

### B-1. 선택적 소음 제거 (음향 담당) ★ 축 B의 우선순위 1위

- [ ] `selective/band_filter.py` — 주파수 대역 기반 통과 판정
- [ ] `selective/pattern_filter.py` — 주파수가 규칙적으로 오르내리는 시간 패턴 검출
- [ ] 분석 창 길이를 변인으로 **잡음 감쇠량(dB)** 과 **검출 지연(ms)** 측정
- [ ] 두 방식의 오검출률 비교 (같은 대역의 방해음 조건)
- [ ] (여유 시) 마이크 2개 도달 시차로 방향 추정 → 주파수 필터가 실패하는 조건에서 방향 필터가 성공하는지 확인

**완료 조건:** 창 길이에 따른 감쇠량–지연 곡선 1장. 두 판정 방식의 오검출률 비교표.

**물리 연계:** 창 길이 Δt와 주파수 분해능 Δf 사이의 맞바꿈. **M1에서 심볼 길이를 줄였을 때 오류율이 오른 것과 같은 제약이다.** 두 그래프를 같은 축에 겹치는 것이 M5의 핵심 결과.

**생명 연계 (문헌):** 달팽이관의 병렬 주파수 분석 구조, 양이 시차를 이용한 공간 필터링(칵테일 파티 효과). 기계는 주파수로 거르고 사람은 방향으로 거른다.

### B-2. 감각의 틈 (가시광 담당)

- [ ] 화면 깜빡임 주파수를 올려가며 사람의 인지 한계 측정 (팀원 대상, 주관 보고)
- [ ] 같은 조건에서 카메라의 복원 한계 측정
- [ ] 두 한계 사이 구간의 존재 여부 확인
- [ ] (도전) 카메라 순차 주사(롤링 셔터) 특성으로 프레임률보다 빠른 신호 복원

**완료 조건:** 인지 한계와 기계 한계를 같은 축에 표시한 그래프 1장.

**물리 연계:** 롤링 셔터. 시간 정보가 공간 좌표로 변환되는 구조라 QR의 공간 부호화와 개념적으로 이어진다.

**생명 연계 (문헌):** 임계 융합 주파수의 종간 차이. 감각의 시간 분해능이 곧 그 종의 정보 대역폭.

---

## M5 — 통합과 발표 (9/7 ~ 9/10)

- [ ] 섀넌 채널 용량 이론값 계산, 실측치와 비교
- [ ] **축 A와 축 B 곡선을 같은 축에 겹쳐 동일 제약임을 보이기** ← 발표의 절정
- [ ] 생물학적 대조 정리 (문헌 기반)
- [ ] `demo/` — 시연 스크립트 (실험 코드와 분리, 실패해도 우아하게). 목표 수준과 3단계
      구현 계획은 `docs/DEMO_UI_PLAN.md` 참고 (2026-08-22, SNS에서 참고 영상 2개를 보고
      팀이 목표로 정함 — DECIMEN류 QR 전송 통계 패널, SOUNDCHAT류 파형·로그 UI)
  - [x] `demo/acoustic_chat.py` — 실시간 음향 채팅 데모 (2026-08-21, 일정보다 앞서 프로토타입만 미리 만듦).
        SOUNDCHAT류 앱에서 영감. `acoustic_fsk.py`는 그대로 두고, 백그라운드 스레드로 항상
        마이크를 듣다가(반이중, 자기 반향은 (시드,내용)으로 걸러냄) 타이핑하면 바로 내보내는
        방식. `ChatReceiver` 판정 로직은 `tests/test_acoustic_chat.py`로 검증(4개 통과) —
        실제 마이크·스레드로 두 기기 간 시연은 아직 학생이 안 해봄
  - [x] **Tier 1 (필수) — `rich` 터미널 HUD, `acoustic_chat.py` 쪽 절반 완료 (2026-08-22).**
        `demo/demo_hub.py`(상태 저장소 `DemoHub`)와 `demo/hud_rich.py`(순수 함수
        `render_panel()`)를 만들고 `acoustic_chat.py`에 얹었다 — `ChatReceiver` 판정
        로직은 안 건드림. 메시지를 보내거나 받을 때마다 패널을 새로 찍는 방식(상시
        갱신 `Live`는 `input()`과 충돌 위험이 있어 이번 범위에서 제외, hud_rich.py
        설명 참고). 테스트 9개 추가(`test_demo_hub.py` 5개, `test_hud_rich.py` 4개),
        전체 64개 통과. **아직 안 한 것:** 실제 마이크로 두 기기 간 시연 — 여전히 학생
        확인 필요. `screen_qr_demo.py`(DECIMEN류 통계 패널)는 미착수
  - [ ] **Tier 2 (권장) — matplotlib 실시간 파형 창.** 이미 쓰는 라이브러리라 새로 배울 것 없음.
        `DemoHub`의 최근 오디오/디코드 이벤트를 `FuncAnimation`으로 그린다. 시간 남으면
  - [ ] **Tier 3 (도전, 확장 과제급) — 로컬 웹 페이지(DECIMEN처럼 폰 브라우저로 봄).**
        Flask + Server-Sent Events. 팀이 안 다뤄본 스택이라 **M4가 끝나고도 시간이 남을 때만**
        착수 — 확장 채널(E1~E3)과 같은 급으로 취급, 실패해도 프로젝트는 무너지지 않는다
  - [ ] `demo/screen_qr_demo.py` 신설 — DECIMEN처럼 프레임마다 capture fps/decode fps/dropped/
        goodput/elapsed를 `DemoHub`에 갱신. `screen_qr.py`는 건드리지 않는다(규칙 5, 채널 공정성)
- [ ] 최종 보고서
- [ ] 발표 자료

**완료 조건:** 인터넷 없는 노트북 두 대로 시연 성공. 리허설 최소 2회.

**시연 설계 원칙:** 실시간 측정에 의존하지 않는다. 실패해도 미리 녹화한 영상으로 이어갈 수 있게 준비한다. 발표장은 조명도 소음도 통제되지 않는다.

---

## 확장 과제 (M4가 끝나고 시간이 남을 때만)

**하나라도 M2~M5를 지연시키면 즉시 중단한다.**

**E1 — 근초음파 (난이도 하, 우선)**
음향 채널의 반송 주파수를 18~20kHz로 올린 변형. M1 코드에서 설정값만 바꾸면 되므로 비용 대비 효과가 가장 좋다. 성능은 가청 대역보다 나쁘지만 **사람이 신호의 존재를 알아채지 못한다** — 축 B의 B-2와 같은 논지를 소리에서 반복하는 셈이라 결론이 두꺼워진다.

**E2 — 진동 채널 (난이도 중)**
폰 진동모터로 송신, 다른 기기 가속도계로 수신. 음향과 같은 종파인데 매질이 고체다. **같은 파동 종류 안에서 매질만 바꾼 통제 비교**가 되는 것이 가치. 접촉 방식이라 거리 변인 설계를 다시 해야 한다.

**E3 — 자기장 채널 (난이도 상, 도전)**
코일에 저주파 신호를 흘려 송신, 폰 자기계로 수신. 전송률은 초당 수 비트로 매우 낮지만 **종이와 벽을 통과한다.** 소리와 빛이 차단되는 조건에서 유일하게 살아남는 채널이라는 점이 발표의 결정적 장면. 폰 자기계 표본율이 낮아 성공을 보장할 수 없다. **실패하면 실패했다고 기록하고 넘어간다.**

---

## 위험 요소

| 위험 | 대응 |
|---|---|
| 카메라 프레임 동기화 실패 | M3의 QR을 포기하고 밝기 변조만. 그것도 안 되면 음향 단독으로 결론까지 |
| 마이크·스피커 기기별 특성 차이 | 측정 기기를 하나로 고정하고 기종을 기록. 기기 비교는 확장 과제로 |
| 발표장 소음으로 시연 실패 | 녹화 영상 백업. 근초음파 대신 가청 대역을 쓰면 성공률이 높다 |
| 팀원 진도 격차 | 채널이 독립 모듈이므로 한 명이 막혀도 나머지는 진행. 주 1회 통합 시점 고정 |
| 축 B가 축 A를 잠식 | 축 B는 M3 이후에만 착수. M2 이전에 손대지 않는다 |
| 일정 초과 | 확장 채널 → B-2 → B-1 순으로 버린다. M5는 줄이지 않는다 |
| 데모 UI 욕심이 M3~M4를 잠식 | 데모는 M5 전용 작업. Tier 1(rich)까지만 필수, Tier 2·3은 확장 과제와 같은 급으로 취급해 시간 없으면 버린다(`docs/DEMO_UI_PLAN.md`) |
